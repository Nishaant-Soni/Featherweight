# Copy to `.env` (git-ignored) and fill in. Loaded via python-dotenv.

# Hugging Face: dataset/model download (avoids rate limits; enables optional push).
HF_TOKEN=

# OpenAI: GPT-4o baseline run through the BFCL harness (PRD §3).
OPENAI_API_KEY=

# Optional experiment tracking. PRD §5 uses MLflow (local file store by default),
# so W&B is optional.
WANDB_API_KEY=
